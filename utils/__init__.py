"""Utils package initializer - ensures `utils` is a proper package for imports.

This file can remain empty, but having it avoids some editor/IDE import warnings
and ensures `from utils.face_verification import ...` works consistently.
"""

__all__ = ["face_verification", "object_detection"]
